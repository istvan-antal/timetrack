import React = require('react');

interface ComponentProps {

};

interface ComponentState {

};

export = class MainWindow extends React.Component<ComponentProps, ComponentState> {
    render() {
        return (
            <div>React initialized!</div>
        );
    }
};
